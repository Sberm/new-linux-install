# ================  IMAP ====================
set imap_user = howardchu95@gmail.com
set imap_pass = "pmdf eplw fome sunx"
set spoolfile = imaps://imap.gmail.com/INBOX
set folder = imaps://imap.gmail.com/
set record="imaps://imap.gmail.com/[Gmail]/Sent Mail"
set postponed="imaps://imap.gmail.com/[Gmail]/Drafts"
set mbox="imaps://imap.gmail.com/[Gmail]/All Mail"
set header_cache = "~/.mutt/cache/headers"
set message_cachedir = "~/.mutt/cache/bodies"
set certificate_file = "~/.mutt/certificates"
# ================  SMTP  ====================
set smtp_url = "smtp://howardchu95@smtp.gmail.com:587/"
set smtp_pass = "pmdf eplw fome sunx"
set ssl_force_tls = yes
# ================  Composition  ====================
set editor = "vim"
set edit_headers = yes
set charset = UTF-8
unset use_domain
set realname = "Howard Chu"
set from = "howardchu95@gmail.com"
set use_from = yes

set sort = reverse-date-received # desc date order

# use vim for reading emails
# set pager = vim

# don't save sent emails in mutt
set copy = no 

# reply all
bind index,pager R group-reply
